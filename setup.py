from setuptools import setup, find_packages

setup(
    name="DualMap",
    version="0.1.0",
    description="A project for dualmap system experiments and evaluation.",
    author="Ying-Yuan",
    packages=find_packages(),
    install_requires=[
    ],
    python_requires='>=3.10',
)